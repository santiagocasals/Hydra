package me.acru.language;

public class LanguageManager {
	
	public static String language;
	
	public static void checkLang() {
		if(!language.equalsIgnoreCase(null) || !language.isBlank() || !language.isEmpty()) {
			
		}
	}
}
